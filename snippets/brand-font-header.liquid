{% if preload %}
  {%- comment -%}
    Preload the primary font weights for better performance
    Using mundial font from Adobe Typekit for headers
    NOTE: Add the following font files to the assets folder:
    - mundial-light.woff2
    - mundial-regular.woff2
    - mundial-semibold.woff2
  {%- endcomment -%}
  {{ 'mundial-regular.woff2' | asset_url | preload_tag: as: 'font', type: 'font/woff2', crossorigin: true }}
  {{ 'mundial-semibold.woff2' | asset_url | preload_tag: as: 'font', type: 'font/woff2', crossorigin: true }}
{% else %}
  {%- comment -%}
    Adobe Typekit "mundial" font family implementation for headers
    Available weights: 300 (light), 400 (normal), 600 (semi-bold)
    Using woff2 format for optimal performance and compatibility

    Font files needed in assets folder:
    - mundial-light.woff2 (weight: 300)
    - mundial-regular.woff2 (weight: 400)
    - mundial-semibold.woff2 (weight: 600)
  {%- endcomment -%}

  {%- comment -%} Mundial Light (300) for headers {%- endcomment -%}
  @font-face { font-display: block; font-family: "mundial"; src: url("{{ 'mundial-light.woff2' | asset_url }}")
  format("woff2"); font-style: normal; font-weight: 300; }

  {%- comment -%} Mundial Regular (400) for headers {%- endcomment -%}
  @font-face { font-display: block; font-family: "mundial"; src: url("{{ 'mundial-regular.woff2' | asset_url }}")
  format("woff2"); font-style: normal; font-weight: 400; }

  {%- comment -%} Mundial Semi-Bold (600) for headers {%- endcomment -%}
  @font-face { font-display: block; font-family: "mundial"; src: url("{{ 'mundial-semibold.woff2' | asset_url }}")
  format("woff2"); font-style: normal; font-weight: 600; }

  {%- comment -%}
    Map font-weight 500 (medium) to 600 (semi-bold) for compatibility
    Map font-weight 700 (bold) to 600 (semi-bold) for compatibility
  {%- endcomment -%}
  @font-face { font-display: block; font-family: "mundial"; src: url("{{ 'mundial-semibold.woff2' | asset_url }}")
  format("woff2"); font-style: normal; font-weight: 500; }

  @font-face { font-display: block; font-family: "mundial"; src: url("{{ 'mundial-semibold.woff2' | asset_url }}")
  format("woff2"); font-style: normal; font-weight: 700; }
{% endif %}
